{
  "name": "expense-splitter-bot",
  "version": "1.0.0",
  "description": "Expense Splitter Bot — WhatsApp + Web (Vercel)",
  "private": true,
  "scripts": {
    "dev": "node dev-server.js",
    "dev:web": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "cors": "^2.8.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
